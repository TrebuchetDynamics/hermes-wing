{
  "version": 1,
  "target": "playwright/probes",
  "updatedAt": "2026-05-31T06:40:38.229Z",
  "objective": "Refactor playwright/probes around existing responsibility folders (screens/, navigation/, features/, support/) while making shared-code reuse the main objective. Current topology keeps README.md as root documentation and support/ as the contract/helper package for probe-local runtime plus shared semantics snapshot helpers.",
  "completedSlices": [
    "Created support/semantics_snapshot.mjs with focused probe contracts: visibleSemanticItems, visibleSemanticTextLines, visibleSemanticButtons, and logQuotedLines.",
    "Re-exported those contracts from support/probe_runtime.mjs.",
    "Reused the new contracts across screens probes (probe_all_screens.mjs, probe_e2e.mjs, probe_routes.mjs), navigation probes (probe_deep.mjs, probe_detail.mjs, probe_navrail.mjs), and feature probes (probe_features.mjs, probe_remaining.mjs, probe_remaining2.mjs, probe_untested.mjs)."
  ],
  "nextCandidates": [
    "Remaining inline DOM queries in probes are interaction-specific selectors/counters (click coordinates, switches, tabs, element counts) rather than identical shared snapshot behavior; no safe generic extraction candidate remains inside playwright/probes."
  ],
  "validationReceipts": [
    "folder_refactor_scan playwright/probes: root files README.md; root dirs features, navigation, screens, support.",
    "node --check for every playwright/probes/**/*.mjs completed successfully."
  ]
}
