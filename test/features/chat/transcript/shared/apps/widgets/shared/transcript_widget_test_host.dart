import 'package:flutter/widgets.dart';

import '../../scaffold/transcript_test_scaffold.dart';

/// Hosts focused transcript widget test subjects in the shared feature-test shell.
Widget transcriptWidgetTestHost(Widget body) => transcriptTestScaffold(body);
